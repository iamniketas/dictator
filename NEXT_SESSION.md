# Dictator — План работ (2026-01-30)

> **Текущий статус:** Стриминг работает, overlay отображается, приложение функционально  
> **Цель:** Полировка, исправление мелких багов, улучшение UX

---

## ✅ Уже реализовано (v0.2.0-alpha)

### Стриминг транскрипции
- ✅ Polling каждые 3 секунды через отдельный поток
- ✅ Partial text отображается в overlay в реальном времени
- ✅ Final text передаётся после остановки записи
- ✅ Корректная очистка буфера между записями

### Overlay UI
- ✅ Win32 окно с прозрачностью (layered)
- ✅ REC индикатор с пульсацией
- ✅ Отображение partial и final текста
- ✅ Drag & drop для перемещения
- ✅ Потокобезопасная архитектура (winit event loop)

### Основной функционал
- ✅ Горячая клавиша Ctrl+Shift+D
- ✅ Запись аудио через cpal
- ✅ Транскрипция через Whisper
- ✅ Коррекция через Ollama
- ✅ Вставка текста в активное окно

---

## 🔧 Задачи на текущую сессию

### 1. Исправить examples/test_overlay.rs
**Проблема:** Использует несуществующие методы `set_text()` и `position_near_cursor()`

**Действие:**
- Исправить на использование `show()` и `update_partial_text()`
- Добавить `set_position()` если нужно

**Проверка:**
```bash
cargo run --example test_overlay
```

---

### 2. Добавить позиционирование окна рядом с курсором
**Проблема:** Окно всегда появляется в фиксированной позиции (100, 100)

**Действие:**
- Добавить метод `position_near_cursor()` в `OverlayWindow`
- Получать позицию курсора через `GetCursorPos`
- Вычислять позицию окна (с отступом от курсора)
- Учитывать границы экрана

**API:**
```rust
pub fn position_near_cursor(&self) {
    // Получить позицию курсора
    // Вычислить позицию окна (под курсором или рядом)
    // Отправить команду в event loop
}
```

---

### 3. Cleanup: убрать warnings
**Список warnings:**
- Неиспользуемые импорты (`anyhow`, `LPARAM`, `LRESULT`, `WPARAM`, `Mutex`)
- Необработанные `Result` в GDI вызовах
- Неиспользуемые поля (`done`, `state`)

**Действие:**
- Удалить неиспользуемые импорты
- Добавить `let _ = ` для неважных результатов
- Добавить `#[allow(dead_code)]` где уместно

---

### 4. Удалить устаревшие файлы
**Файлы для удаления:**
- `src/overlay.rs` — заменён на `overlay_win32.rs`
- `examples/async_test.rs` — не актуален

**Действие:**
```bash
git rm src/overlay.rs
git rm examples/async_test.rs
```

---

## 📋 Чеклист

- [ ] Исправить `examples/test_overlay.rs`
- [ ] Добавить `position_near_cursor()` в overlay
- [ ] Интегрировать позиционирование в `main.rs`
- [ ] Убрать warnings (`cargo fix`)
- [ ] Удалить устаревшие файлы
- [ ] Проверить `cargo build --release`
- [ ] Протестировать полный цикл

---

## 🚀 Будущие улучшения (не в текущей сессии)

### UX
- Настройки overlay в `config.toml` (размер, позиция, шрифт)
- Статусы в overlay: "🎤 Запись...", "⏳ Расшифровка..."
- Автоскрытие через N секунд

### Технические
- VAD для определения пауз (вместо фиксированного интервала)
- Connection pooling для HTTP запросов
- Ограничение размера аудио буфера

### Фичи
- Поддержка нескольких языков
- Отмена записи (Esc при записи)
- История диктовок

---

## 📝 Примечания

- **Сначала тест, потом интеграция** — проверяй изменения перед коммитом
- **Проверяй логи** — используй `RUST_LOG=debug` при тестировании
- **Откатывай при проблемах** — `git checkout -- <file>`

---

*Обновлено: 2026-01-30*
